# GitHub API token — used server-side only. Optional: without it the
# site falls back to static project data (rate limits apply without auth).
# Create one at https://github.com/settings/tokens (no scopes needed for public data)
GITHUB_TOKEN=

# Public site URL — used for sitemap, robots.txt, and social metadata
NEXT_PUBLIC_SITE_URL=
